(ns dayXX)

(defn parse-input [input]
  (throw (RuntimeException. "Not yet implemented")))

(defn read-input []
  (parse-input (slurp "inputs/dayXX.txt")))

(defn TEMP_part1-name [TEMP_parsed-name]
  (throw (RuntimeException. "Not yet implemented")))

(defn -main []
  (let [TEMP_parsed-name (read-input)]
    (println "Part 1:" (TEMP_part1-name TEMP_parsed-name))))
